# Used for signal support in Flask
blinker==1.9.0
# Command line interface creation, used by Flask
click==8.3.0
# The main web framework for the backend API
Flask==3.1.2
# Provides cryptographic helpers for Flask
itsdangerous==2.2.0
# Templating engine for Flask
Jinja2==3.1.6
# Safely handles string escaping for Jinja2 templates
MarkupSafe==3.0.2
# Numerical computing library, useful for data processing
numpy==2.3.3
# WSGI utility library for Flask
Werkzeug==3.1.3

# For image processing (open, manipulate images)
Pillow
# For OCR (extract text from images using Tesseract)
pytesseract
# For file and directory operations in test scripts
glob2
